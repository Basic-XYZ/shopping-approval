# 开发约定

- 原生微信小程序，后端 CloudBase；不要改为 HTML/PWA 或自建服务器。
- 用户自己的业务设计优先；当前是可二开的双人购物审批基础版。
- 前端不写密钥、不信任客户端身份，不允许直接写数据库。
- 改审批/绑定行为时更新 `tests/` 与 `docs/ARCHITECTURE.md`。
- 提交前运行 `npm run verify`；有微信编译器时加 `npm run check:native`。
- AppID 未配置或云环境未关联时，不声明已联调或部署完成。
- 云端写入、费用、Git 推送和正式发布按当前用户授权范围执行。
